# 進捗管理表: 店舗間移動カメラスキャンUI/UXおよび未登録商品処理の改善

このタスクリストは、店舗間移動・物品使用機能におけるカメラスキャンUI/UXと未登録商品の仮登録・同期ロジックの改善進捗を追跡するためのものです。

- [ ] `[ ]` 未完了
- [/] `[/]` 進行中
- [x] `[x]` 完了

---

## 📋 タスクリスト

### 1. 設計とドキュメント作成
- [x] 実装計画書 (`implementation_plan.md`) の作成
- [x] 進捗管理表 (`task.md`) の作成

### 2. カメラスキャンステーションのUI/UX実装
- [x] `JanCodeScannerField.tsx` のリファクタリング
  - [x] 1画面スキャンステーション用のレイアウト（左映像、右リアルタイムリスト）の構築
  - [x] 大きな「カメラを閉じる」ボタンの実装
  - [x] Web Audio API による「ピッ」という超軽量ビープ音再生ロジックの実装
  - [x] スキャン成功時の画面フラッシュ（CSSアニメーション）の実装
  - [x] 1.5秒の連続読み取り防止クールダウンとプログレスバー表示の実装
- [x] `TransferFormModal.tsx` のリファクタリング（UIレイアウト連携）
  - [x] カメラ起動時にスキャンステーション用の全画面（または広域）レイアウトに切り替わるようにする

### 3. 未登録商品・JANコードの自動合算処理
- [x] `TransferFormModal.tsx` 内の `unmatchedItems` の合算ロジック実装
  - [x] スキャンしたJANコードがすでに `unmatchedItems` に存在する場合は数量を `+1` する
  - [x] `items` 側も同様に、重複JANコードがある場合は別々の行にせず数量を合算する
  - [x] スキャンステーション内のリアルタイム積み上げリストに表示するマージロジックの実装

### 4. データベースへの仮商品登録ロジックの実装
- [x] `app/actions/transfers.ts` の `createTransfersAction` 修正
  - [x] 一括登録処理の直前に、登録対象商品の中で `products` テーブルに存在しないJANコードを一括で「仮商品」として `products` テーブルに upsert（既存レコードは無視）する処理を追加

### 5. 移動履歴画面での最新マスタ動的結合
- [x] `lib/queries/transfers.ts` の `fetchTransferHistory` 修正
  - [x] 履歴取得後、それらのJANコードに合致する最新の商品マスタデータを一括フェッチ
  - [x] 履歴の `product_name` と `cost_price` を最新マスタの情報で動的に上書きして返すマッピングを実装

### 6. テストと検証（基本機能）
- [x] 手動でのスキャンステーションUI/UX確認（ビープ音、フラッシュ、クールダウン）
- [x] JANコード重複時の自動合算動作の確認
- [x] 未登録商品の仮登録と、後日のマスタ同期（CSVアップロード）による履歴表示名の自動上書き反映の検証
- [x] `npm run build` によるビルドのパス確認
- [x] 完了ドキュメント (`walkthrough.md`) の作成

### 7. 追加ご要望のUI/UXブラッシュアップ
- [x] `TransferFormModal.tsx` の修正
  - [x] **物品使用区分の非表示制御**: 店舗間移動時（`entryType === 'transfer'`）は「物品使用の区分」セレクトボックスを非表示にし、3カラムに綺麗に並ぶようにする。
  - [x] **不要な説明文の削除**: 「JAN コードから検索して登録リストに積み上げます。」を削除する。
  - [x] **背景スクロールロック**: モーダル起動時に背面の `body` スクロールをロックし、閉じたら元に戻す。
  - [x] **スキャン時レイアウトの小画面最適化**:
    - [x] `isScannerActive === true` 時はモーダル高さを `h-[90vh]` に収めて `overflow-hidden flex flex-col` とする。
    - [x] スキャンした商品一覧（積み上げリスト）の `h-[380px]` 固定高さを削除し、親の残り高さいっぱいに伸縮（`flex-1 h-full min-h-0`）させ、一覧領域のみを独立スクロール（`overflow-y-auto`）とする。
- [x] `JanCodeScannerField.tsx` の修正
  - [x] **カメラ映像のサイズ制限**: 内部の `<video>` 要素に最大高さ（`max-h-[140px] md:max-h-[240px]`）を設定し、スマホなどの小画面でカメラプレビューがはみ出すのを防止。
- [x] 追加機能のビルド＆テスト
  - [x] `npm run build` の実行
  - [x] 実機やエミュレータ、あるいはブラウザ幅縮小による小画面表示のレイアウト崩れ対策の確認。
  - [x] `walkthrough.md` の更新
