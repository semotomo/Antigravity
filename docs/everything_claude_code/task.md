# everything-claude-codeの導入と最適化タスク

- [ ] ドキュメントやリポジトリ構成の確認（完了）
- [x] everything-claude-codeのクローン処理 (`git clone`)
- [x] パッケージのインストール (`npm install`)
- [x] Antigravity (現在のプロジェクト) 用の最適化・ルール生成 (`install.ps1 --target antigravity python`)
- [x] Codex向けの最適化・同期（`.codex/config.toml`等の設定および同期スクリプトの実行）
- [x] 導入後の動作確認
