# タスクリスト (Task List) - ABC分析グラフ機能の実装

本ドキュメントは、ABC分析画面へのリッチな売上貢献度グラフおよび構成比グラフ実装の進捗管理を行うためのToDoリストです。

---

## 📅 進捗状況

- `[ ]` 依存ライブラリの導入
  - `[ ]` `recharts` のインストールおよび型定義の確認
  - `[ ]` ローカル開発環境での動作確認（コンパイルにエラーが出ないか）
- `[ ]` グラフコンポーネントの作成
  - `[ ]` `next_app/components/sales/AbcAnalysisCharts.tsx` の新規作成
  - `[ ]` TOP15商品の横棒グラフ（BarChart）の実装（ランクに応じた色分け）
  - `[ ]` ランク別売上シェアのドーナツチャート（PieChart）の実装（中央に総売上を表示）
  - `[ ]` インタラクティブなツールチップおよびレスポンシブコンテナの実装
- `[ ]` ABC分析画面の改修
  - `[ ]` `next_app/app/(dashboard)/sales/abc/page.tsx` への `AbcAnalysisCharts` の組み込み
  - `[ ]` フィルター連動する集計データの流し込み
- `[ ]` 検証とデプロイ
  - `[ ]` `npm run build` によるビルドチェックのパス
  - `[ ]` 変更ファイルの Git ステージング・コミット・プッシュ（本番デプロイ）
  - `[ ]` 完了報告書（`walkthrough.md`）の作成
